server:
  port: ${PORT:2333}
  address: 0.0.0.0

lavalink:
  server:
    password: "fahad123"

    # IMPORTANT: disable built-in YouTube (plugin will replace it)
    sources:
      youtube: false
      soundcloud: true
      http: true
      bandcamp: true
      twitch: true
      vimeo: true

    # recommended stability settings
    bufferDurationMs: 400
    youtubePlaylistLoadLimit: 6
    playerUpdateInterval: 5

plugins:
  youtube:
    enabled: true

logging:
  level:
    root: INFO